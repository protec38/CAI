version: '3'
services:
  cai:
    build: .
    ports:
      - "5001:5000"
    environment:
      - SECRET_KEY=fvszfgggzepgkôgz
      - ENTREE_USER=entree
      - ENTREE_PASS=test
      - SORTIE_USER=sortie
      - SORTIE_PASS=test
    volumes:
      - .:/app
    restart: unless-stopped



