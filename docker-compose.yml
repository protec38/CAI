version: '3'
services:
  cai:
    build: .
    ports:
      - "5001:5000"
    environment:
      - SECRET_KEY=fvszfgggzepgkôgz
      - ENTREE_USER=lucas
      - ENTREE_PASS=test
      - SORTIE_USER=nico
      - SORTIE_PASS=test
    volumes:
      - .:/app
    restart: unless-stopped

